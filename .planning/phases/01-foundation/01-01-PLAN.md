---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - rokit.toml
  - wally.toml
  - default.project.json
  - .luaurc
  - stylua.toml
  - selene.toml
autonomous: true
requirements:
  - DEVX-03

must_haves:
  truths:
    - "rokit.toml pins rojo, wally, stylua, selene to exact versions"
    - "wally.toml declares charlesnwabuike/ezinput with correct include list"
    - "default.project.json maps src/ as the package root with name EZInput"
    - ".luaurc sets strict language mode project-wide"
    - "stylua.toml and selene.toml use Roblox community standard settings"
  artifacts:
    - path: "rokit.toml"
      provides: "Toolchain version pinning"
      contains: "rojo-rbx/rojo@7.6.1"
    - path: "wally.toml"
      provides: "Wally package manifest"
      contains: "charlesnwabuike/ezinput"
    - path: "default.project.json"
      provides: "Rojo project structure"
      contains: "EZInput"
    - path: ".luaurc"
      provides: "Luau strict mode config"
      contains: "strict"
    - path: "stylua.toml"
      provides: "StyLua formatter config"
      contains: "column_width"
    - path: "selene.toml"
      provides: "Selene linter config"
      contains: "roblox"
  key_links: []
---

<objective>
Create the complete Rokit + Rojo + Wally project scaffold with all toolchain configuration files.

Purpose: Establish the package identity, toolchain versions, and code quality settings that every subsequent plan depends on. Without these config files, stylua/selene cannot run and wally cannot package.
Output: Six config files at project root — rokit.toml, wally.toml, default.project.json, .luaurc, stylua.toml, selene.toml
</objective>

<execution_context>
@/Users/charlesnwabuike/.claude/get-shit-done/workflows/execute-plan.md
@/Users/charlesnwabuike/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create toolchain and package config files</name>
  <files>rokit.toml, wally.toml, default.project.json, .luaurc, stylua.toml, selene.toml</files>
  <action>
Create all six configuration files at the project root. Use the exact values from research:

**rokit.toml** — Pin tools to exact versions:
```toml
[tools]
rojo = "rojo-rbx/rojo@7.6.1"
wally = "UpliftGames/wally@0.3.2"
stylua = "JohnnyMorganz/StyLua@2.3.1"
selene = "Kampfkarren/selene@0.29.0"
```

**wally.toml** — Package manifest (per user decision: scope charlesnwabuike/ezinput):
```toml
[package]
name = "charlesnwabuike/ezinput"
version = "0.1.0"
description = "Fluent chainable builder API for Roblox Input Action System"
license = "MIT"
authors = ["charlesnwabuike"]
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"
include = ["default.project.json", "src", "wally.toml"]
exclude = ["**"]

[dependencies]
```

**default.project.json** — Minimal Wally library pattern. Name MUST be "EZInput" (this becomes the ModuleScript name in consumer's Packages/ folder):
```json
{
  "name": "EZInput",
  "tree": {
    "$path": "src"
  }
}
```

**.luaurc** — Strict mode project-wide (DEVX-01):
```json
{
  "languageMode": "strict"
}
```

**stylua.toml** — Roblox community standard:
```toml
column_width = 120
indent_type = "Tabs"
indent_width = 4
quote_style = "AutoPreferDouble"
```

**selene.toml** — Roblox standard library:
```toml
std = "roblox"
```
  </action>
  <verify>
    <automated>cd "/Users/charlesnwabuike/Documents/Roblox Projects/EZInput" && test -f rokit.toml && test -f wally.toml && test -f default.project.json && test -f .luaurc && test -f stylua.toml && test -f selene.toml && echo "All 6 config files exist"</automated>
  </verify>
  <done>All six config files exist at project root with correct content: rokit.toml pins rojo/wally/stylua/selene, wally.toml declares charlesnwabuike/ezinput with include list, default.project.json names the package EZInput with $path pointing to src, .luaurc sets strict mode, stylua.toml and selene.toml use Roblox community defaults</done>
</task>

<task type="auto">
  <name>Task 2: Install Rokit tools and verify toolchain</name>
  <files></files>
  <action>
Run `rokit install` from the project root to download all pinned tools (rojo, wally, stylua, selene) from rokit.toml.

After installation, verify each tool is available:
- `rojo --version` should output 7.6.x
- `wally --version` should output 0.3.x
- `stylua --version` should output 2.3.x
- `selene --version` should output 0.29.x

Also run `selene generate-roblox-std` to create the roblox.yml standard library file. This file should be committed so CI does not need internet access to resolve Roblox globals.

If rokit is not installed on this machine, note that in the summary — the config files are still correct and CI will install via setup-rokit action. The config files are the deliverable, not local tool installation.
  </action>
  <verify>
    <automated>cd "/Users/charlesnwabuike/Documents/Roblox Projects/EZInput" && (rokit --version 2>/dev/null && rokit install 2>&1 | tail -5 || echo "Rokit not installed locally — config files are correct; CI will use setup-rokit action")</automated>
  </verify>
  <done>Either: (a) all four tools installed and verified via rokit install, roblox.yml generated and ready to commit, OR (b) rokit not available locally — documented in summary, config files are correct for CI</done>
</task>

</tasks>

<verification>
- All 6 config files exist at project root
- wally.toml contains `name = "charlesnwabuike/ezinput"` and `include` list with src
- default.project.json name is "EZInput" (not repo name or anything else)
- .luaurc sets strict mode
- rokit.toml pins all 4 tools to exact versions
</verification>

<success_criteria>
A developer cloning this repo can run `rokit install` and get all toolchain tools at the correct versions. The wally.toml is valid for `wally publish`. The Rojo project file maps src/ correctly for Wally consumers.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
